$(function(){
	var tabs = $("#tabs").iframeTabs({'wrapperClass':'tab-wrapper', 'height':680});
	  $("a[name='supplier-manage']").click(function(event){
		  event.preventDefault();
		  var title = $(this).text();
		  tabs.addTab('supplier-manage', title, 'supplier');
      });
});
